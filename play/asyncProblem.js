const fetchData = (callBackFunc) => {setTimeout(() => {
    console.log('Timer is done')  
  },2000)};
  
setTimeout(() => {
    console.log('Timer is done')
},2000);

console.log('Hi');
console.log('Hey');
  
  // Hi and Hey will be printed first, no waiting for the time first